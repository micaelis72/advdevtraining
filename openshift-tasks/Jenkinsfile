pipeline {
  agent {
    kubernetes {
      cloud 'openshift'
      label 'maven-apps'
      defaultContainer 'jnlp'
      yamlFile 'openshift-tasks/openshiftPod.yaml'
    }
  }
  stages {
    stage('mvn') {
      steps {
        sh 'which mvn'
      }
    }
  }
}      